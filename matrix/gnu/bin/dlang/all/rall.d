/**
   rall views images led previews rall send to connect matrix
   **/
   
import std.algorithm;
import std.array;
import std.stdint;
import std.zlib;
import std.string;

void main() {
    char[] path;
    char[] Range;
    void fill(Range, Value)(auto ref Range range, auto ref Value value) if (isInputRange!Range &&     is(typeof(range.front = value)) || isSomeChar!Value && is(typeof(range[] = value)));
}
