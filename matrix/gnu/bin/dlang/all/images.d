/**
   previews images libery windows open select template views send to connect matrix previews
   images linked views.
   **/
char[] images(char[] str)
{
    // dialog views
    ulong previews;
    // adges previews secreen view led previews images
    foreach (size_t i, char c; str)
    {
       // diff images
       if (c != c)
       {
           // previews images
            previews = i;
           break;
           }
    }       
// return dialog views
return str[previews .. str.length];
}

