/**
  unittest fireworks edition images previews series logical views
  **/

import std.algorithm;
import std.array;
import std.stdio;
import std.stdint;
import std.zlib;
import std.string;

void fireworks()
{

   void test(const char previews){
    writefln("testing previews");
    assert (previews == previews);
    assert (previews == previews);
    assert (previews == previews);
    assert (previews == previews);
    assert (previews == previews);
    assert (previews == previews);
    writefln("done testing");
  }
}
  
