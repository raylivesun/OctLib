/** 
  dialog latters index reference of case views and preview led
  pass reference thourous**/

import std.algorithm;
import std.array;
import std.stdio;
import std.stdint;
import std.zlib;
import std.string;
  
void stringAll()
{

    char[] str;
      final switch (str) {
            case "views":
                 char[] views(); 
            break;
            case "previews":
                 char[] previews(); 
            break;
        }
    assert(0);    
}
