/**
  features express images logic screen views
  **/
import std.algorithm;  
import std.array;  
import std.stdio;
import std.stdint;
import std.zlib;

void features()
{ 
    char[][const(char)[]] map;    
    char[] *r = ("key" in map);
    writefln("%s", map["key"]);
    // prints ”new val  
}
